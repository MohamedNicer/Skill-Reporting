<mvc:View
    controllerName="com.ndbs.skillreportingui.controller.EmployeeSkills"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:core="sap.ui.core"
    xmlns:smartFilterBar="sap.ui.comp.smartfilterbar"
    xmlns:smartTable="sap.ui.comp.smarttable"
>
    <Page id="EmployeeSkills" showHeader="false">
        <FlexBox justifyContent="Center" alignItems="Center">
            <VBox width="95%">
                <smartFilterBar:SmartFilterBar
                    id="sfbEmployeeSkills"
                    entitySet="VEmployeeSkills"
                    enableBasicSearch="true"

                />
                <smartTable:SmartTable
                    id="stEmployeeSkills"
                    entitySet="VEmployeeSkills"
                    smartFilterId="sfbEmployeeSkills"
                    tableType="ResponsiveTable"
                    enableAutoBinding="true"
                    enableExport="true"
                    useVariantManagement="false"
                    useTablePersonalisation="true"
                    showFullScreenButton="true"
                    showRowCount="true"
                    editTogglable="false"
                    header="{i18n>employeeSkills}"
                    initiallyVisibleFields="employeeName,skillName,categoryName,proficiencyLevel,yearsExperience,validationStatusText"

                >
                    <smartTable:customToolbar>
                        <OverflowToolbar design="Transparent">
                            <ToolbarSpacer />
                            <Button
                                id="btnRequestNewSkill"
                                text="{i18n>requestNewSkill}"
                                press="onRequestNewSkill"
                                visible="true"
                            />
                            <OverflowToolbarButton
                                id="btnAddSkill"
                                icon="sap-icon://add"
                                tooltip="{i18n>addSkill}"
                                press="onAddSkill"
                                visible="true"
                            />
                            <OverflowToolbarButton
                                id="btnUpdateSkillDetails"
                                icon="sap-icon://edit"
                                tooltip="{i18n>updateSkillDetails}"
                                press="onUpdateSkillDetails"
                                enabled="false"
                                visible="true"
                            />
                            <OverflowToolbarButton
                                id="btnRemoveSkill"
                                icon="sap-icon://delete"
                                tooltip="{i18n>removeSkill}"
                                press="onRemoveSkill"
                                enabled="false"
                                visible="true"
                            />
                        </OverflowToolbar>
                    </smartTable:customToolbar>
                    <Table
                        id="tblEmployeeSkills"
                        sticky="ColumnHeaders,HeaderToolbar"
                        selectionChange="onEmployeeSkillSelectionChange"
                        mode="SingleSelectLeft"
                    />
                </smartTable:SmartTable>
            </VBox>
        </FlexBox>
        <footer>
            <OverflowToolbar id="otbFooter">
                <Button
                    id="btnMessages"
                    icon="sap-icon://alert"
                    text="{=${message>/}.length}"
                    type="Emphasized"
                    press="onMessagePopoverPress"
                />
            </OverflowToolbar>
        </footer>
    </Page>
</mvc:View>
