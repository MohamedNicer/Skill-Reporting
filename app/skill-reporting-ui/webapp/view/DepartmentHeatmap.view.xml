<mvc:View
    controllerName="com.ndbs.skillreportingui.controller.DepartmentHeatmap"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:core="sap.ui.core"
    xmlns:smartFilterBar="sap.ui.comp.smartfilterbar"
    xmlns:smartTable="sap.ui.comp.smarttable"
>
    <Page id="DepartmentHeatmap" showHeader="true" title="{i18n>departmentHeatmap}" showNavButton="true" navButtonPress=".onNavBack">
        <VBox fitContainer="true" class="sapUiResponsiveContentPadding">
            <!-- Placeholders for Phase 2 Backend Implementation -->
            <MessageStrip 
                text="The Department Heatmap backend view is not yet implemented. This UI will be bound to VDepartmentHeatmap in Phase 2."
                type="Information"
                showIcon="true"
                class="sapUiSmallMarginBottom" />
        </VBox>
        <footer>
            <OverflowToolbar id="otbFooter">
                <Button
                    id="btnMessages"
                    icon="sap-icon://alert"
                    text="{=${message>/}.length}"
                    type="Emphasized"
                    press="onMessagePopoverPress"
                />
            </OverflowToolbar>
        </footer>
    </Page>
</mvc:View>
