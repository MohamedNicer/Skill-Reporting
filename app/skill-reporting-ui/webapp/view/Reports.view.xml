<mvc:View
    controllerName="com.ndbs.skillreportingui.controller.Reports"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:core="sap.ui.core"
    xmlns:smartFilterBar="sap.ui.comp.smartfilterbar"
    xmlns:smartTable="sap.ui.comp.smarttable"
>
    <Page id="Reports" showHeader="false">
        <IconTabBar id="itbReports" expandable="false" class="sapUiResponsiveContentPadding">
            <items>
                <IconTabFilter icon="sap-icon://business-objects-experience" text="{i18n>departmentHeatmap}">
                    <VBox width="100%">
                        <!-- Placeholders for Phase 2 Backend Implementation -->
                        <MessageStrip 
                            text="The Department Heatmap backend view is not yet implemented. This UI will be bound to VDepartmentHeatmap in Phase 2."
                            type="Information"
                            showIcon="true"
                            class="sapUiSmallMarginBottom" />
                    </VBox>
                </IconTabFilter>

                <IconTabFilter icon="sap-icon://activity-assigned-to-goal" text="{i18n>cvActivity}">
                    <VBox width="100%">
                        <MessageStrip 
                            text="The CV Activity backend view is not yet implemented. This UI will be bound to VCVActivity in Phase 2."
                            type="Information"
                            showIcon="true"
                            class="sapUiSmallMarginBottom" />
                    </VBox>
                </IconTabFilter>
            </items>
        </IconTabBar>
        <footer>
            <OverflowToolbar id="otbFooter">
                <Button
                    id="btnMessages"
                    icon="sap-icon://alert"
                    text="{=${message>/}.length}"
                    type="Emphasized"
                    press="onMessagePopoverPress"
                />
            </OverflowToolbar>
        </footer>
    </Page>
</mvc:View>
